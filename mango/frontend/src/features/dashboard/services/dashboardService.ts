import { api } from "@/src/lib/http/axios";

/**
 * Service for general dashboard statistics.
 * If specific summary endpoints are missing, consider using individual feature services.
 */
export const dashboardService = {
  // Add dashboard specific endpoints here as they become available on the backend
};
